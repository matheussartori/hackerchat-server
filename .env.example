# Server port
PORT=9898

# Log level: debug | info | warning | error
# debug: logs all messages as they pass through the server
# info: logs connections, joins, and disconnections
# warning: logs unexpected events and recoverable issues
# error: logs failures only
LOG_LEVEL=info
